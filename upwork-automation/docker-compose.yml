services:
  db:
    image: postgres:latest
    container_name: ibm_sales_navigator_postgres
    restart: always
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - backend
    env_file:
      - .env

  app:
    build: .
    container_name: ibm_sales_navigator_api
    restart: always
    ports:
      - "8001:8001" 
    depends_on:
      - db
    networks:
      - backend
    env_file:
      - .env

volumes:
  postgres_data:

networks:
  backend:
